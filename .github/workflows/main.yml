name: DOOM Project CI/CD

on:
  push:
    branches: [ "main", "Atualizações" ]
  pull_request:
    branches: [ "main" ]

# Força o uso do Node.js 24 para evitar avisos de depreciação
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install pnpm
      uses: pnpm/action-setup@v4
      # Removi a linha 'version: 10' para evitar o conflito com o package.json

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 22
        cache: 'pnpm'

    - name: Install dependencies
      run: pnpm install

    - name: Build project
      run: pnpm build
      env:
        VITE_APP_ID: ${{ secrets.VITE_APP_ID }}
        DATABASE_URL: ${{ secrets.DATABASE_URL }}
